import {generateConstants} from '../../constants/generateConstants';
import {commonTypes} from '../../constants/AppConstants';
export const listfeedConstant = generateConstants(commonTypes, 'LISTFEED');
